apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudwave-api.fullname" . }}-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "cloudwave-api.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
